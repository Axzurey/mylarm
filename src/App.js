import logo from './logo.svg';
import './App.css';

const fs = require(window.require("fs"))
const path = require(window.require("path"))

function App() {
  return (
	<div className="App">
      	<title>
			mylarm
      	</title>
    </div>
	)
}

export default App;
